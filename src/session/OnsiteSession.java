package session;

import attendance.AttendanceList;

public class OnsiteSession extends Session{
    public OnsiteSession(String title, String date, AttendanceList attendanceList) {
        super(title, date, attendanceList);
    }
}
